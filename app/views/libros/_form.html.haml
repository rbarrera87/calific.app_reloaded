= simple_form_for @libro, :html => { :class => 'form-horizontal' } do |f|
  = f.input :nombre, label: 'Nombre:'
  = f.input :editorial, label: 'Editorial:'
  = f.input :autor, label: 'Autor:'
  = f.input :categoria, label: 'Categoría'
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), libros_path, :class => 'btn'
