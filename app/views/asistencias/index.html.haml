%h1 Listing asistencias

%table
  %tr
    %th Presente
    %th
    %th
    %th

  - @asistencias.each do |asistencia|
    %tr
      %td= asistencia.presente
      /%td= link_to 'Show', asistencias/
      %td= link_to 'Edit', edit_asistencia_path(asistencia)
      /%td= link_to 'Destroy', asistencia, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Asistencias', new_asistencia_path
