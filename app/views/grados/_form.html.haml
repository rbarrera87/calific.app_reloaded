= simple_form_for @grado, html: { class: 'form-horizontal' } do |f|
  = f.input :nombre, label: 'Nombre:'
  = f.input :descripcion, label: 'Descripción:'
  .form-actions
    = f.button :submit, class: 'btn-primary'
    = link_to t('.cancel', default: t("helpers.links.cancel")), grados_path, class: 'btn'
