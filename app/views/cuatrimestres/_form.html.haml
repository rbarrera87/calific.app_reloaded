= simple_form_for @cuatrimestre, :html => { :class => 'form-horizontal' } do |f|
  = f.input :nombre
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), cuatrimestres_path, :class => 'btn'
